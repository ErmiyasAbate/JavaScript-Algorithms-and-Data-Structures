//In JavaScript, we can divide our code into reusable parts called functions.
function functionName() {
    console.log("Hello world");
}

// We can call or invoke this function by using its name followed by parentheses
functionName();